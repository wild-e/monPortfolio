<form class="not-a-form" method="post" action="{{ path('admin_message_delete', {'id': message.id}) }}" onsubmit="return confirm('Veux-tu vraiment faire ça ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
    <button class="btn first">Supprimer</button>
</form>
