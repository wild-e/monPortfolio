{% extends 'base.html.twig' %}

{% block title %} 
    {{ project.title }} | {{ parent() }}
{% endblock %}

{% block body %}

    <h4>{{ project.title }}</h4>
    
    <small>{{ project.addDate|date("d.m.Y") }}</small>
    {{ project.text|raw }}

{% endblock %}
